[phases.setup]
nixPkgs = ["python311", "ffmpeg", "libopus", "python311Packages.pip"]

[phases.install]
cmds = [
  "pip install --upgrade pip",
  "pip install davey==0.1.0",
  "pip install -r requirements.txt"
]

[start]
cmd = "python main.py"